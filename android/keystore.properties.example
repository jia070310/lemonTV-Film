# 复制为 android/keystore.properties（与仓库中 android/.gitignore 一致，勿提交 Git）
# 若使用脚本生成：powershell -NoProfile -ExecutionPolicy Bypass -File scripts/create-android-release-keystore.ps1

storePassword=你的密钥库密码
keyPassword=你的密钥密码
keyAlias=lemon_release
storeFile=lemon-release.jks
